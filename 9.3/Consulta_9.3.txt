Consulta 1 ? Manter a mesma.

Consulta 2 ? select * from boletim where (current_date - data_roubo)>365;

Consulta 3 ? Select nome from pessoa inner join boletim on (pessoa.id = boletim.fk_pessoa_id) WHERE (boletim.fk_tipo_roubo_cod_tipo < 2) OR (pessoa.id < 300000);

Consulta 4 ? SELECT * FROM endereco WHERE fk_logradouro_cod_logradouro IS NOT NULL;

Consulta 5 ? SELECT * FROM entrega WHERE cod_entrega > 7700 AND cod_destino = 900;

Consulta 6 ? SELECT * FROM boletim WHERE hora_roubo > '15:00:00' AND id > 1000000;

Consulta 7 ? SELECT data_roubo FROM boletim WHERE fk_tipo_roubo_cod_tipo = 1 OR hora_roubo < '15:00:00';

Consulta 8 ? SELECT cod_tipo AS codigo FROM tipo_roubo;

Consulta 9 ? SELECT tipo AS Tipo_de_Roubo FROM tipo_roubo;

Consulta 10 ? SELECT fk_pessoa_id AS id_pessoa FROM juridica; - MANTER A MESMA PORA

Consulta 11 ? SELECT fk_logradouro_cod_logradouro AS cod_logradouro FROM endereco; - MANTER A MESMA PORRA